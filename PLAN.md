# Workspace Plan Index

The active implementation plan is maintained inside the application repository:

[`_source/Code_Garden_Source_Package_v1/repo/PLAN.md`](_source/Code_Garden_Source_Package_v1/repo/PLAN.md)

That file contains the current stage, stage-status snapshot, acceptance criteria, evidence,
and gate protocol. Do not use this outer file as a second execution plan.

The long-form roadmap is the single canonical execution plan at
[`docs/EXECUTION_PLAN.md`](docs/EXECUTION_PLAN.md). The nested repo's `PLAN.md` is only a live
stage tracker and must not introduce a competing roadmap.
