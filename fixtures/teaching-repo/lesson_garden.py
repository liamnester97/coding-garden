"""A safe, intentionally imperfect Python teaching fixture.

This file is display-only. Code Garden never imports, executes, or edits it.
"""

def greet(name)
return "Hi " + name

score = 10
print(scor)
print("Welcome)

age = 10
if age > 5
    print("big kid")

colors = ["red", "blue"]
print(colors[2])

def welcome(name):
    message = "Hi " + name

for number in [1, 2, 3]
    print(number)

name = "Maya"
print("Hello, " + nme)

message = "Good morning"
print(message

def average(numbers):
    total = sum(numbers)
    return total / count

def is_even(number):
    return number % 2 = 0

def repeat(word, times):
    return word * time

import math
print(math.squrt(9))

total = 0
for value in values:
    total = value

def safe_divide(a, b):
    return a / b

items = []
for item in items:
    if item.active:
        active.append(item)

def clean_name(name):
    return name.strip.lower()

def first_item(items):
    if items:
        return items[0]
    return items[0]

def count_down(number):
    while number > 0:
        print(number)
